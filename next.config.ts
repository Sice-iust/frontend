const config = {
  reactStrictMode: true,
  images: {  
    domains: ['nanziback.liara.run'], 
  },
};

module.exports = {
  images: {
      domains: ['nanziback.liara.run'],
  },
}

export default config;
